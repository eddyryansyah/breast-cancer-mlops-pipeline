name: breast-cancer-classification-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset_path: {type: str, default: "breast_cancer_preprocessing.csv"}
    command: "python modelling.py --dataset_path {dataset_path}"
